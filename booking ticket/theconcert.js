const zoomplus = () => {
  document.getElementById("zoomplus").click();
};

const selectZone = () => {
  document.getElementsByClassName("row zone m-0")[0].click();
};

const buynow = () => {
  document.getElementsByClassName("btn btn-submit")[1].click();
};
